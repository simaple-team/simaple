---
kind: DefaultActiveSkill
version: simaple.io/DefaultActiveSkill
metadata:
  label:
    group: str_based
    name: 메이플 용사
patch:
  - SkillLevelPatch
  - EvalPatch
data:
  name: 메이플 용사
  default_skill_level: 30
  stat:
    STR: "{{ math.floor((18 + 5 * character_level) * 0.15) }}"
---
kind: DefaultActiveSkill
version: simaple.io/DefaultActiveSkill
metadata:
  label:
    group: dex_based
    name: 메이플 용사
patch:
  - SkillLevelPatch
  - EvalPatch
data:
  name: 메이플 용사
  default_skill_level: 30
  stat:
    DEX: "{{ math.floor((18 + 5 * character_level) * 0.15) }}"
---
kind: DefaultActiveSkill
version: simaple.io/DefaultActiveSkill
metadata:
  label:
    group: int_based
    name: 메이플 용사
patch:
  - SkillLevelPatch
  - EvalPatch
data:
  name: 메이플 용사
  default_skill_level: 30
  stat:
    INT: "{{ math.floor((18 + 5 * character_level) * 0.15) }}"
---
kind: DefaultActiveSkill
version: simaple.io/DefaultActiveSkill
metadata:
  label:
    group: luk_based
    name: 메이플 용사
patch:
  - SkillLevelPatch
  - EvalPatch
data:
  name: 메이플 용사
  default_skill_level: 30
  stat:
    LUK: "{{ math.floor((18 + 5 * character_level) * 0.15) }}"
