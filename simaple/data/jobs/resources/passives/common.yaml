---
kind: PassiveSkill
version: simaple.io/PassiveSkill
metadata:
  label:
    group: str_based
    name: 메이플 용사
patch:
  - SkillLevelPatch
  - ArithmeticPatch
data:
  name: 메이플 용사
  stat:
    STR: "{{ floor((18 + 5 * character_level) * 0.15) }}"
---
kind: PassiveSkill
version: simaple.io/PassiveSkill
metadata:
  label:
    group: dex_based
    name: 메이플 용사
patch:
  - SkillLevelPatch
  - ArithmeticPatch
data:
  name: 메이플 용사
  stat:
    DEX: "{{ floor((18 + 5 * character_level) * 0.15) }}"
---
kind: PassiveSkill
version: simaple.io/PassiveSkill
metadata:
  label:
    group: int_based
    name: 메이플 용사
patch:
  - SkillLevelPatch
  - ArithmeticPatch
data:
  name: 메이플 용사
  stat:
    INT: "{{ floor((18 + 5 * character_level) * 0.15) }}"
---
kind: PassiveSkill
version: simaple.io/PassiveSkill
metadata:
  label:
    group: luk_based
    name: 메이플 용사
patch:
  - SkillLevelPatch
  - ArithmeticPatch
data:
  name: 메이플 용사
  stat:
    LUK: "{{ floor((18 + 5 * character_level) * 0.15) }}"
